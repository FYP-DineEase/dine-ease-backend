apiVersion: apps/v1
kind: Deployment
metadata:
  name: map-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: map-mongo
  template:
    metadata:
      labels:
        app: map-mongo
    spec:
      containers:
        - name: map-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: map-mongo-srv
spec:
  selector:
    app: map-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
