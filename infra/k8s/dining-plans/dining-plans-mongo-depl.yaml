apiVersion: apps/v1
kind: Deployment
metadata:
  name: dining-plans-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dining-plans-mongo
  template:
    metadata:
      labels:
        app: dining-plans-mongo
    spec:
      containers:
        - name: dining-plans-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: dining-plans-mongo-srv
spec:
  selector:
    app: dining-plans-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
